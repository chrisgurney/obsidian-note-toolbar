/**
 * Shows how to use JS Engine's markdown renderer to render markdown.
 *
 * Must use an Output callout ID for this to render into your note.
 * The 'engine' variable provides access to JS Engine's API.
 */

return engine.markdown.create('*test*');